# caspardp.github.io

Personal academic website of Caspar David Peter, built with [Quarto](https://quarto.org) and served by GitHub Pages from `docs/`.

Build: `Rscript config.R` (once, installs R packages), then `quarto render`.
